<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nexus Dynamic Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        :root {
            --bg-color: #030303;
            --glass-bg: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.12);
            --text-primary: #ffffff;
            --text-secondary: rgba(255, 255, 255, 0.5);
            --input-bg: rgba(255, 255, 255, 0.05);
            --btn-bg: #ffffff;
            --btn-text: #000000;
            --margin: 20px;
        }

        [data-theme="light"] {
            --bg-color: #f0f4f8;
            --glass-bg: rgba(255, 255, 255, 0.45);
            --glass-border: rgba(0, 0, 0, 0.08);
            --text-primary: #0f172a;
            --text-secondary: #475569;
            --input-bg: rgba(0, 0, 0, 0.04);
            --btn-bg: #0f172a;
            --btn-text: #ffffff;
        }

        body {
            background-color: var(--bg-color);
            margin: 0;
            padding: 0;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            width: 100vw;
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            color: var(--text-primary);
            transition: background-color 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        }

        /* Glassmorphism Frame */
        .glass-container {
            width: calc(100vw - (var(--margin) * 2));
            height: calc(100vh - (var(--margin) * 2));
            background: var(--glass-bg);
            backdrop-filter: blur(50px) saturate(200%);
            -webkit-backdrop-filter: blur(50px) saturate(200%);
            border: 1px solid var(--glass-border);
            border-radius: 32px;
            z-index: 20;
            position: relative;
            display: flex;
            flex-direction: column;
            box-shadow: 0 30px 60px -12px rgba(0, 0, 0, 0.4);
            transition: background 0.5s ease, border 0.5s ease;
        }

        .header {
            width: 100%;
            padding: 24px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 50;
        }

        /* Theme Toggle Button */
        .theme-toggle {
            width: 44px;
            height: 44px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--input-bg);
            border: 1px solid var(--glass-border);
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .theme-toggle:hover {
            transform: scale(1.1);
            background: rgba(255, 255, 255, 0.1);
        }
        .theme-toggle svg {
            width: 20px;
            height: 20px;
            fill: currentColor;
        }

        /* Login Interface */
        .login-content {
            flex-grow: 1;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .input-field {
            width: 100%;
            padding: 14px 18px;
            background: var(--input-bg);
            border: 1px solid var(--glass-border);
            border-radius: 14px;
            color: var(--text-primary);
            outline: none;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .input-field:focus {
            border-color: var(--text-primary);
            background: var(--glass-bg);
        }

        .login-btn {
            width: 100%;
            padding: 16px;
            background: var(--btn-bg);
            color: var(--btn-text);
            font-weight: 700;
            border-radius: 14px;
            transition: transform 0.2s, opacity 0.2s;
            letter-spacing: -0.01em;
        }

        .login-btn:hover { opacity: 0.9; transform: translateY(-2px); }

        /* Optimized Wide-Roaming Background */
        .orb {
            position: absolute;
            border-radius: 50%;
            z-index: 10;
            will-change: transform;
            pointer-events: none;
            filter: saturate(1.5);
        }

        /* Six Orbs with varied sizes and massive roaming ranges */
        .orb-1 { width: 450px; height: 450px; background: radial-gradient(circle, #6366f1, #312e81); animation: roam1 40s linear infinite alternate; top: -10%; left: -10%; opacity: 0.8; }
        .orb-2 { width: 550px; height: 550px; background: radial-gradient(circle, #ec4899, #701a75); animation: roam2 60s linear infinite alternate; top: 40%; left: 80%; opacity: 0.6; }
        .orb-3 { width: 300px; height: 300px; background: radial-gradient(circle, #06b6d4, #083344); animation: roam3 35s linear infinite alternate; top: 80%; left: -5%; opacity: 0.7; }
        .orb-4 { width: 600px; height: 600px; background: radial-gradient(circle, #f59e0b, #9a3412); animation: roam4 55s linear infinite alternate; top: -20%; left: 60%; opacity: 0.5; }
        .orb-5 { width: 200px; height: 200px; background: radial-gradient(circle, #22c55e, #14532d); animation: roam1 50s linear infinite reverse; top: 50%; left: 10%; opacity: 0.8; }
        .orb-6 { width: 350px; height: 350px; background: radial-gradient(circle, #ef4444, #7f1d1d); animation: roam2 45s linear infinite reverse; top: 90%; left: 90%; opacity: 0.6; }

        @keyframes roam1 {
            0% { transform: translate3d(0, 0, 0) rotate(0deg); }
            50% { transform: translate3d(60vw, 40vh, 0) rotate(180deg); }
            100% { transform: translate3d(110vw, -10vh, 0) rotate(360deg); }
        }
        @keyframes roam2 {
            0% { transform: translate3d(0, 0, 0) scale(1); }
            50% { transform: translate3d(-90vw, -30vh, 0) scale(1.2); }
            100% { transform: translate3d(-40vw, 50vh, 0) scale(1); }
        }
        @keyframes roam3 {
            0% { transform: translate3d(0, 0, 0); }
            100% { transform: translate3d(100vw, -100vh, 0); }
        }
        @keyframes roam4 {
            0% { transform: translate3d(0, 0, 0) rotate(0deg); }
            50% { transform: translate3d(-100vw, 100vh, 0) rotate(-180deg); }
            100% { transform: translate3d(-20vw, 20vh, 0) rotate(-360deg); }
        }

        /* Mobile specific orb scaling */
        @media (max-width: 768px) {
            .orb { opacity: 0.4; }
            .orb-1, .orb-2, .orb-4 { width: 250px; height: 250px; }
        }
    </style>
</head>
<body>

    <!-- Background Layer (Vibrant, roaming, hardware-accelerated) -->
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="orb orb-3"></div>
    <div class="orb orb-4"></div>
    <div class="orb orb-5"></div>
    <div class="orb orb-6"></div>

    <div class="glass-container">
        
        <!-- Navigation Header -->
        <header class="header">
            <div class="flex items-center gap-3">
                <div class="w-9 h-9 bg-[var(--text-primary)] rounded-xl flex items-center justify-center transition-colors">
                    <div class="w-4 h-4 bg-[var(--bg-color)] rounded-full transition-colors"></div>
                </div>
                <span class="font-black tracking-tighter text-2xl">NEXUS</span>
            </div>

            <!-- Theme Toggle Button -->
            <button id="themeToggle" class="theme-toggle" aria-label="Toggle Theme">
                <!-- Sun Icon -->
                <svg id="sunIcon" class="hidden" viewBox="0 0 24 24"><path d="M12 7c-2.76 0-5 2.24-5 5s2.24 5 5 5 5-2.24 5-5-2.24-5-5-5zM2 13h2c.55 0 1-.45 1-1s-.45-1-1-1H2c-.55 0-1 .45-1 1s.45 1 1 1zm18 0h2c.55 0 1-.45 1-1s-.45-1-1-1h-2c-.55 0-1 .45-1 1s.45 1 1 1zM11 2v2c0 .55.45 1 1 1s1-.45 1-1V2c0-.55-.45-1-1-1s-1 .45-1 1zm0 18v2c0 .55.45 1 1 1s1-.45 1-1v-2c0-.55-.45-1-1-1s-1 .45-1 1zM5.99 4.58a.996.996 0 00-1.41 0 .996.996 0 000 1.41l1.06 1.06c.39.39 1.03.39 1.41 0s.39-1.03 0-1.41L5.99 4.58zm12.37 12.37a.996.996 0 00-1.41 0 .996.996 0 000 1.41l1.06 1.06c.39.39 1.03.39 1.41 0s.39-1.03 0-1.41l-1.06-1.06zm1.06-10.96a.996.996 0 000-1.41.996.996 0 00-1.41 0l-1.06 1.06c-.39.39-.39 1.03 0 1.41s1.03.39 1.41 0l1.06-1.06zM7.05 18.36a.996.996 0 000-1.41.996.996 0 00-1.41 0l-1.06 1.06c-.39.39-.39 1.03 0 1.41s1.03.39 1.41 0l1.06-1.06z"></path></svg>
                <!-- Moon Icon -->
                <svg id="moonIcon" viewBox="0 0 24 24"><path d="M12 3c-4.97 0-9 4.03-9 9s4.03 9 9 9 9-4.03 9-9c0-.46-.04-.92-.1-1.36-.98 1.37-2.58 2.26-4.4 2.26-3.03 0-5.5-2.47-5.5-5.5 0-1.82.89-3.42 2.26-4.4-.44-.06-.9-.1-1.36-.1z"></path></svg>
            </button>
        </header>

        <!-- Login Card -->
        <main class="login-content">
            <div class="w-full max-w-[380px] px-8">
                <div class="text-center mb-12">
                    <h1 class="text-4xl font-bold tracking-tight mb-3">Sign In</h1>
                    <p class="text-[var(--text-secondary)] font-medium text-sm">Nexus Operating System v4.0</p>
                </div>
                
                <form onsubmit="event.preventDefault()" class="space-y-6">
                    <div class="space-y-1.5">
                        <label class="text-[10px] uppercase tracking-[0.2em] font-black ml-1 opacity-50">Username</label>
                        <input type="text" placeholder="ID / Username" class="input-field" autocomplete="username" required>
                    </div>
                    <div class="space-y-1.5">
                        <label class="text-[10px] uppercase tracking-[0.2em] font-black ml-1 opacity-50">Password</label>
                        <input type="password" placeholder="••••••••" class="input-field" autocomplete="current-password" required>
                    </div>
                    
                    <button type="submit" class="login-btn !mt-10">Access Dashboard</button>
                </form>
            </div>
        </main>

    </div>

    <script>
        const themeToggle = document.getElementById('themeToggle');
        const sunIcon = document.getElementById('sunIcon');
        const moonIcon = document.getElementById('moonIcon');

        // Initial check for theme icons
        const updateIcons = (isDark) => {
            if (isDark) {
                sunIcon.classList.remove('hidden');
                moonIcon.classList.add('hidden');
            } else {
                sunIcon.classList.add('hidden');
                moonIcon.classList.remove('hidden');
            }
        };

        // Theme Switcher Logic
        themeToggle.addEventListener('click', () => {
            const currentTheme = document.documentElement.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            document.documentElement.setAttribute('data-theme', newTheme);
            updateIcons(newTheme === 'dark');
        });

        // Mouse Parallax for slight glass tilting (Performance optimized)
        const container = document.querySelector('.glass-container');
        document.addEventListener('mousemove', (e) => {
            // Very subtle tilt
            const x = (window.innerWidth / 2 - e.pageX) / 100;
            const y = (window.innerHeight / 2 - e.pageY) / 100;
            container.style.transform = `rotateY(${x}deg) rotateX(${y}deg)`;
        });

        // Initialize icons on load
        updateIcons(document.documentElement.getAttribute('data-theme') === 'dark');
    </script>
</body>
</html>